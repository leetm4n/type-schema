export enum PropertyTypes {
  NUMBER = 'number',
  STRING = 'string',
  DATE = 'date',
  BOOLEAN = 'boolean',
  OBJECT = 'object',
  ARRAY = 'array',
}

export enum MetadataKeys {
  TYPE = 'design:type',
  PROPERTIES = 'custom:properties',
  OPTIONS = 'custom:options',
}
